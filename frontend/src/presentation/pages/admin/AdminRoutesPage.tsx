export function AdminRoutesPage() {
  return (
    <div>
      <h1 style={{ fontSize: 'var(--font-size-2xl)', fontWeight: 700, marginBottom: 'var(--space-2)' }}>Rutas del Sistema</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>🚧 Gestión global de líneas y rutas de transporte</p>
    </div>
  );
}
