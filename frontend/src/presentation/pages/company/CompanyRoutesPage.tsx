export function CompanyRoutesPage() {
  return (
    <div>
      <h1 style={{ fontSize: 'var(--font-size-2xl)', fontWeight: 700, marginBottom: 'var(--space-2)' }}>Rutas</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>🚧 Gestión de rutas, paradas y horarios de la empresa</p>
    </div>
  );
}
