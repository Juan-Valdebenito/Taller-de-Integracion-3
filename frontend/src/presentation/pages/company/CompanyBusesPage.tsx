export function CompanyBusesPage() {
  return (
    <div>
      <h1 style={{ fontSize: 'var(--font-size-2xl)', fontWeight: 700, marginBottom: 'var(--space-2)' }}>Gestión de Micros</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>🚧 CRUD de micros, asignación de rutas y monitoreo de ocupación</p>
    </div>
  );
}
